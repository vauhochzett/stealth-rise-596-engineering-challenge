[project]
name = "procurement-api"
version = "0.1.0"
description = "Backend for the procurement requests app."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.124.0",
    "openai>=2.9.0",
]
